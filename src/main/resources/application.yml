server:
  port: 8080
  servlet:
    context-path:

spring:
  datasource:
    url: jdbc:mariadb://svc.sel5.cloudtype.app:32098/mariadb?useSSL=false&verifyServerCertificate=false
    username: root
    password: "020723"
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
    show-sql: true

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

# JWT 비밀 키를 설정합니다.
# 반드시 256비트(32자) 이상의 길이를 가져야 합니다.
jwt:
  secret: 94175e92538f40ada5a7467221567425abcde12345678901234567890abcde
